function range(start, end) {
  let total = [];
  for (let i = start, i = end, i++){
       total.push(i);
  }
}


function reverseArray(array){
  rev = [];
  for (let number of array) { 
  rev.push(array.pop());
  return rev;
  }
}
